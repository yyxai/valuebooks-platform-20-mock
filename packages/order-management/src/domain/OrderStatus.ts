export enum OrderStatus {
  Draft = 'draft',
  CheckingOut = 'checking_out',
  PaymentPending = 'payment_pending',
  Confirmed = 'confirmed',
  Shipped = 'shipped',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
